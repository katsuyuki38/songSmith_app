FROM node:22-slim

ENV NODE_ENV=production
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY server/vocal-server-cloud.js ./server/vocal-server-cloud.js

EXPOSE 8080
CMD ["node", "server/vocal-server-cloud.js"]
